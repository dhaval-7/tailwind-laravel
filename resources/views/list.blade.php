{{-- <div id="app" class="bg-gray-300"> --}}
    {{-- <div class="container mx-auto"> --}}
        <div class="py-20">
            <div class="hover:bg-gray-200 cursor-pointer bg-white shadow flex p-5 items-center mb-5 rounded-lg">
                <div class="w-1/2">
                    <div class="flex items-center">
                        <img :src="contact.picture.thumbnail" class="rounded-full">
                        <div class="ml-4">
                            <span class="capitalize block text-gray-800">{{ $contact ?? 'test' }}</span>
                            {{-- <span class="text-sm block text-gray-600">{{ $contact ?? 'test' }}</span> --}}
                        </div>

                    </div>
                </div>
            </div>

            {{-- </div> --}}

    {{-- </div> --}}
